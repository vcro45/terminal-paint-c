# Terminal Paint

A text-mode drawing program written in C. It supports creating a canvas, drawing horizontal, vertical, and diagonal lines, resizing the board, adding or deleting rows and columns, erasing cells, and saving or loading canvas files.

## Build

```bash
make
```

## Run

```bash
./paint
./paint 20 40
```

Use `h` inside the program to view commands.
